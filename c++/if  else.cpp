#include <iostream>
using namespace std;
int main()
{   int a = 20;
    int b = 20;
    
    // bool x = 0 // все числа, кроме 0 = true
    // только 0 - false
    // a < b -> true 
    // a > b -> false
    // a <= b -> true 
    // a == b -> false 
    // a != b-> true проверка на неравность
    // cout << x;
    // if(a < b){
        // cout << "a is smaller than b";
    // }
    // else if(a > b){ // else - иначе если
            // cout << "a is larger than b";
    // }
    // else{// иначе
        // cout << "a is equal to b";
    //  bool x = (15 > 10) && (12 < 9) && (10 < 20); // and можно писать через && // если один из выражении будет false то будет false
    // cout << x;
    // // or - ||
    //  bool x = (15 > 10) || (12 > 9) || (10 > 20); // если один  из выржении будет false то вывод будет true 
    // cout << x;
    
    // }
